# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/scheduler?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=mansi
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update  
# Use 'update' to auto-create the schema or 'validate' to check
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.format_sql=true  
# Formats SQL in the console (optional)
spring.cache.type=simple



